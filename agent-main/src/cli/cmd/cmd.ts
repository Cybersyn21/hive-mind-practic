// Утилита-обёртка для типизированных команд CLI на основе yargs
// Обеспечивает правильную типизацию при определении команд с аргументами и опциями

import type { CommandModule } from "yargs"

// Функция-обёртка для определения типизированной команды yargs
// Принимает CommandModule и возвращает его как есть, обеспечивая корректную типизацию TypeScript
// T - тип аргументов команды, U - тип опций команды
export function cmd<T, U>(input: CommandModule<T, U>) {
  return input
}
