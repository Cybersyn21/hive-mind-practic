# @link-assistant/agent

**Минималистичный AI CLI-агент с открытым исходным кодом (Public Domain), совместимый с JSON-интерфейсом OpenCode**

[![npm version](https://badge.fury.io/js/@link-assistant%2Fagent.svg)](https://www.npmjs.com/package/@link-assistant/agent)
[![License: Unlicense](https://img.shields.io/badge/license-Unlicense-blue.svg)](http://unlicense.org/)

> 🚨 **ПРЕДУПРЕЖДЕНИЕ О БЕЗОПАСНОСТИ: 100% НЕБЕЗОПАСНО И АВТОНОМНО** 🚨
>
> Этот агент работает **БЕЗ ОГРАНИЧЕНИЙ** и с **ПОЛНОЙ АВТОНОМИЕЙ**:
> - ❌ **Нет песочницы** - Полный неограниченный доступ к файловой системе
> - ❌ **Нет системы разрешений** - Не требуется одобрение для любых действий
> - ❌ **Нет защитных механизмов** - Может выполнить ЛЮБУЮ команду с полными привилегиями
> - ⚠️ **Автономное выполнение** - Принимает решения и выполняет действия самостоятельно
>
> **ИСПОЛЬЗУЙТЕ ТОЛЬКО в изолированных средах** (виртуальные машины, Docker-контейнеры), где AI-агенты могут иметь неограниченный доступ. **НЕ БЕЗОПАСНО** для персональных компьютеров, продакшн-серверов или систем с конфиденциальными данными.

> ⚠️ **Только Bun runtime** - Этот пакет требует [Bun](https://bun.sh) и НЕ поддерживает Node.js или Deno.

Это MVP-реализация OpenCode-совместимого CLI-агента, ориентированного на максимальную эффективность и неограниченное выполнение. Мы воспроизводим режим OpenCode `run --format json --model opencode/grok-code` со следующими возможностями:

- ✅ **JSON Ввод/Вывод**: Совместим с `opencode run --format json --model opencode/grok-code`
- ✅ **Ввод обычного текста**: Также принимает текстовые сообщения (автоматически конвертируются в JSON формат)
- ✅ **Гибкий выбор модели**: По умолчанию бесплатная OpenCode Zen Grok Code Fast 1, поддерживает все [модели OpenCode Zen](https://opencode.ai/docs/zen/)
- ✅ **Без ограничений**: Полностью неограниченный доступ к файловой системе и выполнению команд (без песочницы)
- ✅ **Минимальный размер**: Построен на Bun.sh для максимальной эффективности
- ✅ **Полная поддержка инструментов**: 13 инструментов включая websearch, codesearch, batch - все включены по умолчанию
- ✅ **100% совместимость с OpenCode**: Все выходные данные инструментов точно соответствуют JSON формату OpenCode
- ✅ **Внутренний HTTP-сервер**: Использует локальный HTTP-сервер для управления сеансами (не доступен извне)
- ❌ **Нет TUI**: Только чистый JSON CLI интерфейс
- ❌ **Нет песочницы**: Разработан для VM/контейнеров, где полный доступ приемлем
- ❌ **Нет LSP**: Нет поддержки Language Server Protocol для диагностики
- ❌ **Нет разрешений**: Нет системы разрешений - полный неограниченный доступ
- ❌ **Нет интеграции с IDE**: Нет функций интеграции с IDE/редакторами
- ❌ **Нет плагинов**: Нет системы плагинов
- ❌ **Нет Share**: Нет функции совместного использования сеансов
- ❌ **Нет внешнего API**: Сервер работает только внутренне, не доступен в сети
- ❌ **Нет ACP**: Нет поддержки Agent Client Protocol

## Видение проекта

Мы создаём упрощённую версию OpenCode CLI в общественном достоянии (public domain), ориентированную на "агентный режим выполнения" для использования в виртуальных машинах, Docker-контейнерах и других средах, где приемлем неограниченный доступ AI-агента. Это **не** для общего использования на рабочем столе - это для изолированных сред, где вы хотите максимальной свободы AI-агента.

**Совместимость с OpenCode**: Мы поддерживаем 100% совместимость с форматом потоковой передачи событий JSON OpenCode, поэтому инструменты, ожидающие вывод `opencode run --format json --model opencode/grok-code`, будут работать с нашим agent-cli.

## Проектные решения

### Почему только Bun? Нет поддержки Node.js или Deno?

Этот агент **эксклюзивно построен для Bun** по следующим причинам:

1. **Быстрая разработка**: Нет этапа компиляции - прямое выполнение с `bun run`
2. **Простые зависимости**: Меньше dev-зависимостей, нет накладных расходов компилятора TypeScript
3. **Производительность**: Быстрый runtime Bun и нативная поддержка ESM
4. **Минимализм**: Одна целевая среда выполнения упрощает кодовую базу
5. **Экосистема Bun**: Использует специфичные для Bun функции и оптимизации

**Отсутствие поддержки Node.js или Deno сделано намеренно**, чтобы сохранить проект сфокусированным и минимальным. Если вам нужна совместимость с Node.js/Deno, рассмотрите использование [OpenCode](https://github.com/sst/opencode).

### Архитектура: Воспроизведение потоковой передачи событий JSON OpenCode

Этот agent-cli воспроизводит основную архитектуру команды [OpenCode](https://github.com/sst/opencode) `run --format json`:

- **Потоковые JSON события**: Вместо одиночных ответов выводит поток событий в реальном времени
- **Типы событий**: `tool_use`, `text`, `step_start`, `step_finish`, `error`
- **Управление сеансами**: Каждый запрос получает уникальный ID сеанса
- **Выполнение инструментов**: 13 инструментов с неограниченным доступом (bash, read, write, edit, list, glob, grep, websearch, codesearch, batch, task, todo, webfetch)
- **Совместимый формат**: События соответствуют JSON-схеме OpenCode для взаимодействия

Агент передаёт события по мере их возникновения, обеспечивая тот же опыт работы в реальном времени, что и JSON-режим OpenCode.

## Возможности

- **JSON Ввод/Вывод**: Принимает JSON через stdin, выводит потоки событий JSON (совместим с OpenCode)
- **Ввод обычного текста**: Также принимает текстовые сообщения (автоматически конвертируются в JSON формат)
- **Неограниченный доступ**: Полный доступ к файловой системе и выполнению команд (без песочницы, без ограничений)
- **Поддержка инструментов**: 13 инструментов включая websearch, codesearch, batch - все включены по умолчанию
- **Гибкий выбор модели**: По умолчанию бесплатная Grok Code Fast 1, поддерживает все [модели OpenCode Zen](https://opencode.ai/docs/zen/) - см. [MODELS.md](MODELS.md)
- **Bun.sh First**: Построен на Bun для максимальной эффективности и минимального использования ресурсов
- **Нет TUI**: Чистый JSON CLI интерфейс для автоматизации и интеграции
- **Общественное достояние**: Unlicense - используйте как хотите

## Установка

**Требования:**
- [Bun](https://bun.sh) >= 1.0.0 (Node.js и Deno НЕ поддерживаются)

```bash
# Сначала установите Bun, если ещё не установлен
curl -fsSL https://bun.sh/install | bash

# Установите пакет глобально
bun install -g @link-assistant/agent

# Или установите локально в ваш проект
bun add @link-assistant/agent
```

После установки команда `agent` будет доступна глобально.

## Использование

### Простейшие примеры

**Обычный текст (проще всего):**
```bash
echo "привет" | agent
```

**Простое JSON сообщение:**
```bash
echo '{"message":"привет"}' | agent
```

**С пользовательской моделью:**
```bash
echo "привет" | agent --model opencode/grok-code
```

### Больше примеров

**Ввод обычного текста:**
```bash
echo "hello world" | agent
echo "поиск в интернете новостей о TypeScript" | agent
```

**JSON ввод с вызовами инструментов:**
```bash
echo '{"message":"выполнить команду","tools":[{"name":"bash","params":{"command":"ls -la"}}]}' | agent
```

**Использование разных моделей:**
```bash
# Модель по умолчанию (бесплатная Grok Code Fast 1)
echo "привет" | agent

# Другие бесплатные модели
echo "привет" | agent --model opencode/big-pickle
echo "привет" | agent --model opencode/gpt-5-nano

# Премиум модели (подписка OpenCode Zen)
echo "привет" | agent --model opencode/sonnet        # Claude Sonnet 4.5
echo "привет" | agent --model opencode/haiku         # Claude Haiku 4.5
echo "привет" | agent --model opencode/opus          # Claude Opus 4.1
echo "привет" | agent --model opencode/gemini-3-pro  # Gemini 3 Pro
```

См. [MODELS.md](MODELS.md) для полного списка доступных моделей и цен.

### Опции CLI

```bash
agent [опции]

Опции:
  --model                        Модель для использования в формате providerID/modelID
                                 По умолчанию: opencode/grok-code
  --json-standard                Стандарт формата вывода JSON
                                 Варианты: "opencode" (по умолчанию), "claude" (экспериментально)
  --system-message               Полная замена системного сообщения
  --system-message-file          Полная замена системного сообщения из файла
  --append-system-message        Добавить к системному сообщению по умолчанию
  --append-system-message-file   Добавить к системному сообщению по умолчанию из файла
  --help                         Показать справку
  --version                      Показать номер версии
```

### Стандарты вывода JSON

Агент поддерживает два стандарта формата вывода JSON через опцию `--json-standard`:

#### Стандарт OpenCode (по умолчанию)

Формат OpenCode - это формат вывода JSON по умолчанию, совместимый с `opencode run --format json`:

```bash
echo "привет" | agent --json-standard opencode
```

- **Формат**: Форматированный JSON (читаемый с отступами)
- **Типы событий**: `step_start`, `step_finish`, `text`, `tool_use`, `error`
- **Временные метки**: Unix миллисекунды (число)
- **ID сеанса**: `sessionID` (camelCase)

#### Стандарт Claude (экспериментально)

Формат Claude обеспечивает совместимость с Claude CLI от Anthropic `--output-format stream-json`:

```bash
echo "привет" | agent --json-standard claude
```

- **Формат**: NDJSON (Newline-Delimited JSON - компактный, один JSON на строку)
- **Типы событий**: `init`, `message`, `tool_use`, `tool_result`, `result`
- **Временные метки**: Строки ISO 8601
- **ID сеанса**: `session_id` (snake_case)

### Форматы ввода

**Обычный текст (автоконвертация):**
```bash
echo "ваше сообщение здесь" | agent
```

**Формат JSON:**
```json
{
  "message": "Ваше сообщение здесь",
  "tools": [
    {
      "name": "bash",
      "params": { "command": "ls -la" }
    }
  ]
}
```

## Поддерживаемые инструменты

Все 13 инструментов **включены по умолчанию** и **не требуют конфигурации**. См. [TOOLS.md](TOOLS.md) для полной документации.

### Операции с файлами
- **`read`** - Чтение содержимого файлов
- **`write`** - Запись файлов
- **`edit`** - Редактирование файлов с заменой строк
- **`list`** - Список содержимого директории

### Инструменты поиска
- **`glob`** - Поиск файлов по шаблону (`**/*.js`)
- **`grep`** - Текстовый поиск с поддержкой regex
- **`websearch`** ✨ - Веб-поиск через Exa API (не требует настройки!)
- **`codesearch`** ✨ - Поиск кода через Exa API (не требует настройки!)

### Инструменты выполнения
- **`bash`** - Выполнение shell-команд
- **`batch`** ✨ - Пакетное выполнение нескольких вызовов инструментов (не требует настройки!)
- **`task`** - Запуск подзадач с субагентами

### Утилиты
- **`todo`** - Отслеживание задач
- **`webfetch`** - Получение и обработка URL

✨ = Всегда включено (не требуется экспериментальных флагов или переменных окружения)

## Примеры

См. [EXAMPLES.md](EXAMPLES.md) для подробных примеров использования каждого инструмента с командами agent-cli и opencode.

## Тестирование

```bash
# Запустить все тесты
bun test

# Запустить конкретный тестовый файл
bun test tests/mcp.test.js
bun test tests/websearch.tools.test.js
bun test tests/batch.tools.test.js
bun test tests/plaintext.input.test.js
```

Для подробной информации о тестировании, включая запуск тестов вручную и запуск CI тестов, см. [TESTING.md](TESTING.md).

## Сопровождение

### Разработка

Запустить агента в режиме разработки:
```bash
bun run dev
```

Или запустить напрямую:
```bash
bun run src/index.js
```

### Тестирование

Просто запустите:
```bash
bun test
```

Bun автоматически обнаруживает и запускает все файлы `*.test.js` в проекте.

### Покрытие тестами

- ✅ 13 тестов реализации инструментов
- ✅ Тест поддержки ввода обычного текста
- ✅ Тесты совместимости с OpenCode для websearch/codesearch
- ✅ Модульные тесты стандартов JSON (форматы opencode и claude)
- ✅ Все тесты проходят со 100% совместимостью с JSON форматом OpenCode

### Публикация

Для публикации новой версии в npm:

1. **Обновите версию** в `package.json`:
   ```bash
   # Обновите поле version вручную (например, 0.0.3 -> 0.0.4)
   ```

2. **Зафиксируйте изменения**:
   ```bash
   git add .
   git commit -m "Release v0.0.4"
   git push
   ```

3. **Опубликуйте в npm**:
   ```bash
   npm publish
   ```

Пакет публикует исходные файлы напрямую (не требуется этап сборки). Bun обрабатывает выполнение TypeScript нативно.

## Ключевые возможности

### Не требуется конфигурация
- **WebSearch/CodeSearch**: Работают без переменной окружения `OPENCODE_EXPERIMENTAL_EXA`
- **Batch Tool**: Всегда включён, не требуется экспериментальный флаг
- **Все инструменты**: Нет конфигурационных файлов, API ключи обрабатываются автоматически

### 100% совместимость с OpenCode
- Все инструменты производят вывод JSON, точно соответствующий формату OpenCode
- Инструменты WebSearch и CodeSearch проверены на 100% совместимость
- Структура событий инструментов соответствует спецификациям OpenCode
- Может использоваться как прямая замена для `opencode run --format json`

### Поддержка обычного текста
Работают как обычный текст, так и JSON ввод:
```bash
# Обычный текст
echo "привет" | bun run src/index.js

# JSON
echo '{"message":"привет"}' | bun run src/index.js
```

Обычный текст автоматически конвертируется в формат `{"message":"ваш текст"}`.

### Потоковый вывод событий JSON
Вывод JSON форматирован для лёгкой читаемости при сохранении совместимости с OpenCode:
```bash
echo "привет" | agent
```

Вывод (форматированные JSON события):
```json
{
  "type": "step_start",
  "timestamp": 1763618628840,
  "sessionID": "ses_560236487ffe3ROK1ThWvPwTEF",
  "part": {
    "id": "prt_a9fdca4e8001APEs6AriJx67me",
    "type": "step-start",
    ...
  }
}
{
  "type": "text",
  "timestamp": 1763618629886,
  "sessionID": "ses_560236487ffe3ROK1ThWvPwTEF",
  "part": {
    "id": "prt_a9fdca85c001bVEimWb9L3ya6T",
    "type": "text",
    "text": "Привет! Как я могу помочь с вашими задачами по программированию сегодня?",
    ...
  }
}
{
  "type": "step_finish",
  "timestamp": 1763618629916,
  "sessionID": "ses_560236487ffe3ROK1ThWvPwTEF",
  "part": {
    "id": "prt_a9fdca8ff0015cBrNxckAXI3aE",
    "type": "step-finish",
    "reason": "stop",
    ...
  }
}
```

## Архитектура

Этот agent-cli воспроизводит архитектуру команды OpenCode `run --format json`:

- **Потоковые JSON события**: Вывод потока событий в реальном времени
- **Типы событий**: `tool_use`, `text`, `step_start`, `step_finish`, `error`
- **Управление сеансами**: Уникальные ID сеансов для каждого запроса
- **Выполнение инструментов**: 13 инструментов с неограниченным доступом
- **Совместимый формат**: События точно соответствуют JSON-схеме OpenCode

## Файлы

- `src/index.js` - Основная точка входа с поддержкой ввода JSON/обычного текста
- `src/session/agent.js` - Реализация агента
- `src/tool/` - Реализации инструментов
- `tests/` - Комплексный набор тестов
- [MODELS.md](MODELS.md) - Доступные модели и цены
- [TOOLS.md](TOOLS.md) - Полная документация инструментов
- [EXAMPLES.md](EXAMPLES.md) - Примеры использования для каждого инструмента

## Референсные реализации

Этот репозиторий включает официальные референсные реализации как git submodules для предоставления лучших примеров:

- **original-opencode** - [OpenCode](https://github.com/sst/opencode) - Оригинальная реализация OpenCode, с которой мы поддерживаем совместимость
- **reference-gemini-cookbook** - [Google Gemini Cookbook](https://github.com/google-gemini/cookbook) - Официальные примеры и руководства по использованию Gemini API
- **reference-gemini-cli** - [Google Gemini CLI](https://github.com/google-gemini/gemini-cli) - Официальный AI-агент, приносящий Gemini прямо в терминал
- **reference-qwen3-coder** - [Qwen3-Coder](https://github.com/QwenLM/Qwen3-Coder) - Официальная кодовая модель Qwen3 от Alibaba Cloud

Для инициализации всех submodules:
```bash
git submodule update --init --recursive
```

Эти референсные реализации предоставляют ценные идеи о разных подходах к построению AI-агентов и могут служить обучающими ресурсами для разработчиков, работающих с этой кодовой базой.

## Лицензия

Unlicense (Общественное достояние)
